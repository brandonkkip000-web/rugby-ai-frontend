// Note: temporary alias route removed to avoid build-time type errors.
// Keeping this file without exporting `Route` ensures the router plugin ignores it.
export const dashboardAliasDisabled = true
